# 🛡️ Chemical Manager 개발 절대 원칙 (ABSOLUTE PRINCIPLES)

본 문서는 Antigravity AI 코딩 어시스턴트가 프로젝트 코드를 수정하거나 새로운 기능을 구현할 때 **반드시 준수해야 하는 절대적 개발 원칙과 원칙 보존 규정**을 기록합니다.
> **규칙**: 본 문서의 기존 내용은 절대 삭제하거나 축소하지 않으며, 새로운 원칙이 추가될 때마다 하단에 지속적으로 추적 추가(Append Only)합니다.

---

## 📌 [원칙 1] OneDrive 공동편집 및 파일 동시성 제어 (4단계 원칙)

동기화(`sync_engine.py`) 및 DB 수동 업데이트(`db_update_dialog.py`, `save_db_records_win32com`) 시 다음 4단계 동시성 제어 수칙을 엄격히 적용한다.

1. **덮어쓰기 직전 파일 상태 명시적 검사 (Pre-Save Inspection)**
   - 저장을 수행하기 바로 직전에 대상 파일(`ChemicalList.xlsx`)이 다른 사람이나 프로세스에 의해 편집 중인지(`is_file_locked`, ReadOnly 여부) 명시적으로 확인한다.
   - 파일을 메모리에 읽어왔던 시점의 타임스탬프(`mtime`) 및 스냅샷과 덮어쓰기 직전의 파일 타임스탬프를 비교하여, 읽은 이후 외부에서 수정된 곳이 없는지 검사한다.

2. **편집 중 대기 및 자동 재시도 (Backoff Lock Retry)**
   - 만약 대상 파일이 타인에 의해 편집 중이거나 잠겨 있는 경우, 강제 저장하지 않고 일정 시간(지수 백오프/2초~5초 간격) 대기 후 자동 재시도한다.
   - 편집이 완전히 끝나 잠금이 해제된 안전한 시점에 덮어쓰기 작업을 진행한다.

3. **직전 수정 사항 동적 반영 (Diff & 3-Way Merge)**
   - 읽었던 시점과 덮어쓰기 직전 시점 사이에 파일이 타인에 의해 수정된 경우:
     - 덮어쓰기 직전의 최신 파일 구조와 초기 스냅샷을 비교하여 타인이 변경한 셀/행(Diff)을 파악한다.
     - 내 작업 영역과 겹치지 않는 타인의 변경 내용(예: 수령 확인 'O', 장소 변경 등)은 최신 타인 수정본을 먼저 반영(Merge)하여 병합 저장한다.

4. **직접 충돌 시 전체 취소 및 처음부터 재동기화 (Conflict Rollback & Restart)**
   - 타인이 새로 수정한 셀과 내가 이번 동기화/업데이트로 수정하려는 셀이 동일한 경우(Collision):
     - 진행 중인 동기화/업데이트 작업을 **전면 취소(Rollback)**한다.
     - 타인이 수정한 최신 버전의 파일을 다시 로드하여 처음부터 동기화 또는 크롤링/업데이트 작업을 재수행(Restart)한다.

---

## 📌 [원칙 2] 자가 프로세스 강제 사살(Taskkill) 절대 금지

- 동기화 또는 DB 저장 도중 `is_file_locked` 등의 검사 함수에서 현재 작동 중인 자신의 Win32COM Excel 프로세스를 엑셀 프로세스로 오인하여 `taskkill /F /IM EXCEL.EXE`로 강제 종료하는 행위를 엄격히 금지한다.
- 프로세스 강제 종료는 COM RPC 연결을 파괴하여 `AttributeError: Open.Save` / `RPC 서버를 사용할 수 없습니다.` 오류를 발생시키므로, 자신의 PID를 식별하여 예외 처리한다.

---

## 📌 [원칙 3] 함수 유실 방지 및 모듈 수출 보장 (Export Integrity)

- `sync_engine.py` 등 핵심 코드를 리팩토링할 때, 기존 다른 모듈(`main_window.py`, `db_update_dialog.py` 등)에서 임포트하여 사용 중인 공개 함수(`is_file_locked`, `save_db_records_win32com`, `refresh_chemical_list_formatting` 등)를 유실시키거나 삭제하지 않는다.
- 코드 수정 후에는 항상 전체 프로젝트의 `import` 무결성 검사를 자동 실행하여 유실된 심볼이 없는지 검증한다.

---

## 📌 [원칙 4] 사용자 지시 시 코드 수정 동결 (Coding Freeze Directive)

- 사용자가 "코딩 하지 마", "말할 때까지 기다려" 등의 명시적 지시를 내리면, 그 즉시 모든 파일 수정/생성 도구(`replace_file_content`, `write_to_file` 등) 사용을 동결하고 원인 분석 및 대기 모드로 전환한다.
- 사용자의 명시적 승인이 있을 때에만 코드 변경 작업을 재개한다.

---

## 📌 [원칙 5] Implementation Plan 누적 기록 관리 (Append Only)

- 모든 개발 플랜 및 구현 계획서는 작성 후 반드시 `PLANS_HISTORY.md` 파일에 사용자 지시 요구사항과 함께 지속적으로 누적하여 기록(Append Only)한다.
- 이전 계획서 내용을 절대 삭제하거나 덮어쓰지 않으며, Antigravity 코딩 시 반드시 본 원칙 파일(`ABSOLUTE_PRINCIPLES.md`)과 누적 계획서(`PLANS_HISTORY.md`)를 100% 참조하여 코딩한다.

---

## 📌 [원칙 6] 조건부 백업 실행 원칙 (변경사항 발생 시에만 백업)

- 동기화 및 DB 업데이트 시작 시 무조건 백업 파일을 생성하지 않는다.
- 메모리 상에서 파일 비교 및 크롤링 검토를 마친 후, **실제로 파일에 변경/신규/업데이트가 발생하는 경우에만** 저장 직전에 백업(`ChemicalList_backup_...xlsx`)을 진행한다. 동기화할 내용이 0건이면 백업 파일을 생성하지 않는다.

---

## 📌 [원칙 7] 파일 편집 중일 때 1분 대기 및 자동동기화 주기 연동 원칙

- 대상 파일이 타인에 의해 편집 중인 경우(`is_file_locked`), 2초 대기 대신 **1분 간격**으로 대기 후 재시도한다.
- **자동 동기화 설정 시**: 다음 자동 동기화 실행 전까지만 1분 단위 대기를 반복하며, 그래도 잠겨 있으면 해당 회차를 건너뛰고 다음 주기에 재시도한다.
- **자동 동기화 미설정 시**: 사용자가 해제하거나 중지 버튼을 누를 때까지 1분 단위로 무한 대기 반복한다.

---

## 📌 [원칙 8] 수동 동기화 및 DB 업데이트 작업 중지(Cancel) 버튼 제공

- 수동 동기화 실행 및 DB 수동 업데이트 진행 중일 때 실행 버튼을 비활성화하지 않고, **"🛑 동기화 중단"** / **"🛑 DB 업데이트 중단"**으로 변경하여 버튼 클릭을 가능하게 한다.
- 작업 도중 사용자가 해당 버튼을 클릭하면 즉시 진행 중인 크롤링/동기화/대기 스레드를 안전하게 취소(Cancel/Stop)하고 원상 복구한다.

---

## 📌 [원칙 9] 동일 데이터 재크롤링 시 엑셀 재저장 및 백업 남발 차단 원칙

- 이미 DB에 존재하는 시약을 재크롤링할 때, 실질적인 시약 정보(`Product Name`, `CAS No.`, `Storage Temp.`, `Sensitivity`, `Signal Word`, `Key Hazards`, `Detailed Hazard Classification`, `Detail_Link`, `SDS_Link`, `SDS_Local_Path`)에 변경사항이 없다면 **단순 시스템 메타데이터(`Revision Date`, `Key` 등)의 변경을 사유로 엑셀 파일 저장을 시도하거나 "총 N개 반영" 알림을 발생시키지 않는다.**
- 실질 변경 항목이 0건일 경우 엑셀 저장을 즉시 건너뛰고 `"업데이트할 새로운 DB 정보가 없습니다. (기존 데이터와 동일)"` 상태로 안전하게 종료한다.

---

## 📌 [원칙 10] SDS PDF 파일 6개월(180일) 유효 보존 및 재다운로드 차단 원칙

- DB 수동 업데이트 및 동기화 수행 시, 기존 local SDS PDF 파일이 존재하고 그 파일의 수정 일자가 오늘 기준 6개월(180일) 이내인 경우:
  - SDS 링크를 포함한 시약 데이터가 동일하다면 SDS PDF 파일을 새로 다운로드하거나 덮어쓰지 않고 기존 local SDS 파일 경로(`SDS_Local_Path`)를 100% 보존한다.
  - 불필요한 네트워크 트래픽 및 파일 덮어쓰기로 인한 I/O 낭비를 완벽히 차단한다.

---

## 📌 [원칙 11] 원본 오더북 파일 무조건 읽기 전용(ReadOnly=True) 오픈 원칙

- 동기화 엔진(`sync_engine.py`) 실행 시 사용자의 원본 오더북 엑셀 파일은 무조건 **`ReadOnly=True`** 옵션으로만 오픈한다.
- 원본 파일에는 어떠한 셀 수정, 완료 표시 기입, 저장 시도도 하지 않으며, 오직 데이터 로딩 용도로만 사용함으로써 원본 파일 잠금 발생 및 수정을 100% 차단한다.

---

## 📌 [원칙 12] 중단/취소 클릭 시 즉시 0.1초 반응 및 작업 강제 해제 원칙

- 파일 사용 대기 루프(`check_and_wait_lock`), 수동 동기화 루프, DB 수동 업데이트 루프는 **0.1초(100ms) 간격으로 중단 시그널(`is_stopped`, `check_stop_fn`)을 즉각 확인**한다.
- 사용자가 "🛑 중단" 버튼을 클릭하는 즉시 1분 대기를 기다리지 않고 0.1초 이내에 작업을 전면 취소하고 모든 COM/스레드 자원을 즉시 안전하게 반납한다.

---

## 📌 [원칙 13] OneDrive 공유본 장시간 점유 금지 및 키 기반 3-Way Commit 원칙

- `ChemicalList.xlsx` 공유본은 주문서 비교나 웹 크롤링이 진행되는 동안 쓰기 모드로 장시간 열어 두지 않는다.
- 작업 시작 시 공유본을 읽고 스냅샷을 만든 다음 OneDrive 동기화 폴더 밖의 임시 작업 복사본에서만 장시간 작업한다.
- ChemicalList 행은 `Order No.`, DB 행은 정규화된 `Manufacturer|Catalog No.`를 논리 키로 사용하며 저장 시 물리적 행 번호를 신뢰하지 않는다.
- 저장 직전에 최신 공유본을 다시 읽어 `base / ours / theirs` 셀 단위 3-Way 비교를 수행한다.
  - `theirs == base`: 프로그램 변경 적용
  - `ours == base`: 공동편집자 변경 유지
  - `theirs == ours`: 이미 같은 결과이므로 저장 생략
  - 세 값이 모두 다름, 행 삭제 또는 헤더 구조 변경: 직접 충돌로 판정
- 직접 충돌 시 공유 파일에는 아무것도 저장하지 않고 최신본에서 전체 작업을 처음부터 다시 수행한다. 반복 충돌은 제한 횟수 후 안전하게 종료하여 무한 재시작을 막는다.
- 프로그램 내부에서도 수동 동기화, 자동 동기화, DB 업데이트가 동시에 공유 파일을 수정하지 못하도록 단일 쓰기 작업 잠금을 사용한다.

---

## 📌 [원칙 14] 대용량 ChemicalList 행 범위 동적 확장 원칙

- 조건부 서식, 데이터 유효성 검사 및 프로그램 관리 수식의 기본 준비 범위는 최소 3,000행으로 한다.
- 실제 데이터가 2,500행을 초과하면 고정 상한으로 자르지 않고 현재 마지막 데이터 행보다 최소 1,000행 큰 범위까지 자동 확장한다.
- 관리 범위의 끝 행은 항상 500행 단위로 올림하여 관리한다. 예를 들어 마지막 데이터 행이 2,501행이면 최소 여유 1,000행을 확보한 뒤 500행 단위로 올림하여 4,000행까지 관리한다.
- 저장 직전 최신 공유본에는 프로그램이 의도한 셀 패치만 적용하고, 공동편집자가 변경한 기존 셀 값을 광범위한 수식 재주입으로 덮어쓰지 않는다.

---

## 📌 [원칙 15] OneDrive 로컬 동기화 상태와 클라우드 원자성 한계 명시 원칙

- 로컬 OneDrive 폴더를 사용하는 현재 방식의 스냅샷, 파일 지문 및 3-Way Merge는 해당 PC에 내려온 최신 로컬 사본을 기준으로 동작한다.
- OneDrive 동기화가 일시정지되었거나 클라우드 변경이 아직 로컬에 내려오지 않은 상태까지 로컬 파일 검사만으로 감지했다고 간주하지 않는다.
- 운영 시 OneDrive.exe 동기화가 활성 상태이고 오류가 없어야 하며, 서버 리비전까지 엄격한 Compare-And-Swap 보장이 필요하면 향후 Microsoft Graph `eTag`/`cTag` 조건부 저장을 추가한다.
- 위 한계를 로그·문서·테스트 결과에서 숨기지 않으며, 실제 배포 전 서로 다른 두 Microsoft 365 계정으로 공동편집 통합 검증을 수행한다.

---

## 📌 [원칙 16] 크롤링 최소 대기·중복 제거 및 Excel 단일 기록 단계 원칙

- 웹 페이지는 고정 시간만큼 무조건 대기하지 않고 DOM 준비 상태와 필요한 요소를 0.1초 간격으로 확인하여 준비되는 즉시 진행한다.
- 제조사와 카탈로그 번호를 정규화한 키를 사용하여 한 실행에서 동일 제품을 한 번만 크롤링한다.
- 180일 이내의 로컬 SDS가 존재하면 네트워크 요청 전에 이를 확인하고 PDF 탐색·다운로드를 생략한다.
- 크롤링 결과는 메모리에 먼저 수집하며, 브라우저 및 네트워크 작업이 끝난 뒤 Excel DB 기록을 단일 단계에서 수행한다.
- 조건부 대기와 장시간 네트워크 작업에서도 사용자 중단 요청을 0.1초 단위로 확인한다.

---

## 📌 [원칙 17] 운영 종료·배포·공통 식별자 무결성 원칙

- 프로그램 종료 시 자동/수동 동기화, DB 저장, PDF 작업에 중단 신호를 먼저 전달하고 제한 시간 동안 안전 종료를 기다린 뒤 애플리케이션을 종료한다.
- 제조사 정규화, 지원 여부, 스크래퍼 선택은 하나의 공통 레지스트리만 사용하며 각 화면이나 엔진에서 별도 문자열 규칙을 복제하지 않는다.
- 제품 선택과 중복 판단은 카탈로그 번호 단독이 아니라 정규화된 `Manufacturer|Catalog No.` 복합 키를 사용한다.
- `requirements.txt`, 실행 배치, PyInstaller 설정과 실제 import 의존성을 함께 유지하며 깨끗한 환경에서 설치·빌드 가능한 상태를 보장한다.
- 설정 또는 로그 저장 실패를 콘솔 없는 배포본에서 숨기지 않고 사용자에게 명시적으로 알린다.

---

## 📌 [원칙 18] 희소 주문번호 파일의 행 위치 및 DB 수식 무결성 원칙

- ChemicalList의 신규 행 위치를 `Order No.` 열 하나로 판단하지 않는다. 기존 행의 주문번호가 비어 있을 수 있으므로 `Order No.`, `Product Name`, `Original Product Name`, `Manufacturer`, `Catalog No.` 중 실제 마지막 행의 최댓값 다음 행에 추가한다.
- 논리 키 기반 3-way 병합 후에는 최신 공유본의 실제 행 번호와 헤더 이름을 기준으로 프로그램 관리 DB 조회 수식을 다시 생성한다. 작업 복사본의 물리적 행 번호가 포함된 수식을 최신 공유본에 그대로 복사하지 않는다.
- 주문번호가 없는 기존 행도 DB 수식 적용 대상에서 누락하지 않으며, 사용자가 직접 입력한 CAS 값은 빈 값·실패 표시·기존 프로그램 수식인 경우가 아니면 보존한다.
- 회귀 테스트에는 `Order No.`의 마지막 행은 1이지만 기존 시약 데이터는 60행까지 존재하여 신규 시약이 61행에 추가되어야 하는 조건을 반드시 포함한다.

---

## 📌 [원칙 19] Lot 번호 선행 0 보존 및 캐시 동일성 원칙

- Lot 번호의 원본 표시값과 COA 파일명에서는 선행 0을 보존한다.
- Excel이 숫자형 Lot의 선행 0을 제거했더라도 양쪽 Lot가 숫자로만 구성되어 있고 선행 0을 제외한 값이 같으면 동일 Lot로 판정한다.
- 기존 COA PDF의 파일 유효성, 카탈로그 번호 및 Lot 동일성이 확인되면 서명 URL 만료 여부와 관계없이 다시 다운로드하지 않는다.
- 숫자가 아닌 문자가 포함된 Lot는 임의로 0을 제거하지 않고 대소문자만 정규화하여 비교한다.

---

## 📌 [원칙 20] 주문번호 없는 소분·수동 시약명 보존 및 기본 열기 시트 원칙

- `Order No.`가 없는 행에서 사용자가 직접 입력한 `Product Name`은 소분(Aliquot), 자가 합성 또는 수동 관리 명칭으로 간주하여 DB 제품명 수식으로 덮어쓰지 않는다.
- 주문번호 없는 행이라도 Product Name 셀이 이미 프로그램 DB 조회 수식이면 기존 레거시 정규 시약 행으로 간주하여 실제 행 번호와 헤더에 맞게 수식을 갱신한다.
- 소분 시약은 원 시약의 Manufacturer, Catalog No., Lot No. 등 추적 정보를 유지하고, `Product Name`에는 소분 시약을 구분할 명칭을 직접 입력하며 `Remarks`에 `Aliquot` 및 필요한 소분 정보를 기록한다.
- `Remarks`는 사용자 관리 값으로 유지하며 동기화 과정에서 프로그램이 덮어쓰지 않는다.
- ChemicalList.xlsx를 저장할 때 `ChemicalList` 시트를 활성화하여 다음에 파일을 열면 해당 시트가 먼저 표시되도록 한다. 데이터 변경이 없어도 활성 시트가 다르면 이 메타데이터 변경을 한 번 저장한다.

---

## 📌 [원칙 21] ChemicalList 명시 선택·백업 보존기간·배포 아이콘 원칙

- ChemicalList 대상 경로는 설정의 `target_file`을 전체 기능의 단일 기준으로 사용한다. 설정이 없는 기존 사용자에게만 오더북 폴더의 `ChemicalList.xlsx`를 호환 기본값으로 사용한다.
- 동기화, DB 수동 업데이트, PDF 출력, SDS 병합은 서로 다른 방식으로 대상 파일을 재탐색하지 않고 동일한 중앙 경로 결정 함수를 사용한다.
- 사용자가 선택한 대상 파일은 `.xlsx` 형식이며 `ChemicalList`와 `DB` 필수 시트를 포함해야 한다. 원본 오더북과 대상 파일이 같으면 저장을 차단한다.
- 백업 자동 정리는 선택된 ChemicalList 옆의 `backup` 폴더에서 이름이 정확히 `ChemicalList_backup_*.xlsx`인 일반 파일만 처리한다. 현재 시각에서 3개월 전과 같거나 오래된 파일만 삭제하고 다른 파일과 하위 폴더는 건드리지 않는다.
- 소스 실행, 프로그램 창, 시스템 트레이와 배포 EXE는 제공된 Chemical Reagent Manager 아이콘 자산을 공통으로 사용한다. 아이콘 자산은 단일 EXE 내부에 포함한다.

---

## 📌 [원칙 22] 프로그램 로컬 백업 단일 경로 원칙

- 원칙 21의 “선택된 ChemicalList 옆 backup 폴더” 규정은 본 원칙으로 대체한다. 새 백업은 ChemicalList 또는 OneDrive 공유 폴더 아래에 만들지 않는다.
- 모든 백업 생성·조회·보존기간 정리는 로그 폴더와 같은 프로그램 루트를 기준으로 한 `<프로그램 루트>/backup` 단일 경로만 사용한다. 배포 EXE에서는 EXE가 있는 폴더, 소스 실행에서는 `chem_manager_app` 폴더가 프로그램 루트다.
- 백업 생성과 정리는 `backup_manager.py`의 중앙 함수만 사용하며 `sync_engine.py` 등 호출부에서 별도 경로를 조합하지 않는다.
- `copy2`로 원본을 복사한 직후 백업 파일의 수정시각을 실제 백업 생성 시각으로 갱신하여 오래된 원본 수정시각 때문에 새 백업이 조기 삭제되지 않도록 한다.
- 자동 정리는 프로그램 하위 `backup`에서 정확히 `ChemicalList_backup_*.xlsx`인 일반 파일만 대상으로 하며 달력 기준 3개월 전과 같거나 오래된 파일만 삭제한다.
