# 화학물질 관리 및 SDS 자동 수집 프로그램 가이드

## 1. 프로그램 개요

본 프로그램은 연구실 내 **시약 관리의 효율화 및 OneDrive 공동 편집 환경에서의 안전한 통합 시약 리스트(ChemicalList.xlsx) 구축**을 최우선 목적으로 개발된 화학물질 종합 관리 시스템입니다.

### 🌟 주요 지원 제조사

- **Thermo Fisher Scientific** (Thermo / Fisher)
- **Sigma-Aldrich** (Sigma / Aldrich / Merck)
- **TCI** (Tokyo Chemical Industry)
- **Abcam**

### 🎯 핵심 기능 및 우선순위

1. **통합 시약 리스트 관리 (1순위 메인 기능)**
   - 원드라이브(OneDrive) 공유 폴더 상에서 여러 연구원이 동시에 접근하여 시약을 등록, 조회, 수령/사용/폐기 처리하고 보관위치(Room, Cabinet), 수량, 상태를 실시간 공유 관리합니다.
   - 오더북(주문대장)의 신규 입고 정보를 자동으로 반영하고, 자가 합성 시약, 소분(Aliquot) 시약, 중간 행 삽입 항목 등을 비파괴적으로 유연하게 관리할 수 있습니다.

2. **화학물질 정보 자동 크롤링 및 DB 구축 (2순위 자동화 기능)**
   - 주요 4개 제조사의 공식 홈페이지에 접속하여 시약명, CAS 번호, 보관온도, GHS 위험분류, 민감성(Moisture, Light, Air, Heat 등)을 자동 추출하여 데이터베이스(DB 시트)를 구축합니다.

3. **SDS(안전보건자료) PDF 자동 다운로드 및 일괄 병합 (3순위 기능)**
   - 정제된 표준 파일명(`시약명 (제조사, 제품번호).pdf`) 형태로 관련 SDS 원본 PDF 문서를 자동 수집하여 sds/ 폴더에 저장하고, 모든 SDS를 1개의 PDF로 합치는 **일괄 병합 기능**을 제공합니다.

4. **PDF 인쇄용 자동 파일 생성**
   - 시약장 비치 및 실물 현황판용 엑셀 출력물을 PDF 파일로 변환하여 출력할 수 있습니다.

5. **Lot별 COA/CoC 수집 및 이력 연결**
   - 오더북의 `Lot No.`를 기준으로 제조사 공식 COA를 내려받아 `coa/` 폴더에 저장하고, ChemicalList의 `Lot No.`, `Expiration Date`, `COA Link`, `COA Local Path` 열에 연결합니다.
   - 동일 시약을 여러 번 주문해도 주문번호와 Lot별로 서로 다른 문서를 유지합니다. Abcam처럼 COA 대신 CoC를 제공하는 경우 파일명에 `CoC`를 명시하고, COA 링크/경로 셀을 주황색으로 표시하며 메모에 CoC 및 Datasheet 링크와 로컬 경로를 남깁니다.

---

## 2. 시약 리스트 구조 및 데이터베이스 연동 원리

### 📊 ChemicalList.xlsx 시트 구조

* **ChemicalList 시트 (메인 관리 시트)**:
  - 연구원이 시약 현황을 확인하고 보관장소(Room), 보관온도(Storage Temp), 캐비넷(Cabinet), 사용수량(Used), 수령상태(Status), 비고(Remarks) 등을 직접 기재하고 관리하는 메인 시트입니다.

* **DB 시트 (마스터 데이터베이스)**:
  - 크롤링 및 수기로 수집된 화학물질의 마스터 정보(CAS No., GHS 위험분류, 민감성, 기본 보관온도, SDS 웹 링크 등)가 모여 있는 데이터베이스 시트입니다.

### 🔗 헤더 기반 DB 조회 수식 연동 및 텍스트 보존 로직

* **제조사와 카탈로그 번호가 있는 정규 시약**:
  - ChemicalList 시트의 제품명, CAS No., 보관온도(Storage Temp.), 민감성, 위험정보 및 SDS 관련 셀에는 `INDEX/MATCH` 기반 수식이 자동으로 적용되어 DB 시트를 실시간 참조합니다.
  - DB의 `Key`, 제조사, 카탈로그 번호 및 조회 대상 열은 고정 A:N 위치가 아니라 실제 헤더 이름으로 찾습니다. 따라서 DB 열 순서가 바뀌어도 헤더 이름이 유지되면 수식이 올바른 열을 참조합니다.
  - **[보관온도 수동 변경 주의사항]**: 특정 시약의 보관온도를 기본값과 다르게 변경하고 싶다면, ChemicalList 시트가 아니라 **DB 시트에서 해당 시약의 보관온도를 수정**하셔야 수식이 깨지지 않고 정상 반영됩니다.

* **주문번호가 없는 소분 시약 / 자가 합성 시약 / 임의 기재 항목**:
  - `Order No.`가 없고 Product Name이 사용자가 직접 입력한 텍스트이면 DB 제품명 수식을 적용하지 않고 **기존 Product Name을 100% 그대로 유지**합니다.
  - 소분 시약은 원 시약을 추적할 수 있도록 Manufacturer, Catalog No., Lot No. 등의 정보를 그대로 입력해도 됩니다. Product Name에는 소분 시약을 구분할 명칭을 직접 적고, Remarks에는 `Aliquot`과 소분일·농도·용량·원 용기 정보 등 필요한 내용을 기록하세요.
  - Remarks는 사용자 관리 열이므로 동기화가 기존 Aliquot 메모를 삭제하거나 변경하지 않습니다.
  - 주문번호가 없더라도 Product Name 셀이 이미 프로그램 DB 조회 수식인 기존 레거시 정규 시약 행은 실제 위치에 맞게 수식을 갱신합니다.

### 📐 프로그램 관리 범위

* 조건부 서식과 데이터 유효성 검사의 기본 관리 범위는 3,000행입니다.
* 실제 데이터가 2,500행을 초과하면 마지막 데이터 행보다 최소 1,000행 큰 범위를 확보하고, 끝 행을 500행 단위로 올림합니다. 예를 들어 마지막 데이터가 2,501행이면 4,000행까지 관리합니다.
* 신규 시약을 추가할 마지막 데이터 행은 ChemicalList의 `Product Name` 열을 기준으로 판단합니다.

---

## 3. 프로그램 처리 5단계 프로세스 및 사용자 체크리스트

프로그램은 다음 5단계 순서로 작동하며, 각 단계별 사용자 체크 포인트(✅)를 반드시 확인해 주시기 바랍니다.

### Step 1. 오더북 (입력 파일) 읽기 및 로컬 DB 대조

* **프로세스**: 원드라이브 공유 폴더 상의 오더북(주문대장)을 **읽기 전용(ReadOnly=True)** 으로 열어 수령 확인된(`O`, `o`, `ㅇ` 또는 지원 날짜 형식) 신규 항목을 조회하고, DB 시트와 대조합니다.
* ✅ **사용자 체크 포인트**:
  - 다른 사용자가 오더북 엑셀을 열어둔 상태에서도 동기화가 안전하게 진행됩니다.
  - 오더북의 열 이름(주문번호, 날짜, 주문자, 제조사, 품목명, 품번, CAS 번호, 수량 등)이 설정의 매핑 규칙과 일치하는지 확인하세요.

### Step 2. 타겟 제조사 자동 분류

* **프로세스**: 오더북의 제조사가 **Thermo Fisher, Sigma-Aldrich(Aldrich), TCI, Abcam** 4개 타겟 제조사에 해당하는지 자동 판별합니다.
* ✅ **사용자 체크 포인트**:
  - 4개 제조사가 아니거나 자가 합성 시약인 경우 웹 크롤링을 시도하지 않고 결과란에 **"직접 입력하세요"** 로 표기됩니다. 완료 후 연구원이 수기로 정보를 입력하시면 됩니다.

### Step 3. 웹 스크래핑 및 DB 자동 갱신 (크롤링)

* **프로세스**: SeleniumBase 봇 방어 우회 브라우저가 접속하여 규격화된 정보를 추출하고 DB 시트를 갱신합니다:
  - **CAS 번호**: `xxx-xx-x` 정규 표현식 추출.
  - **보관온도**: RT (실온), Refrigerator (4°C), Freezer (-20°C), Deep freezer (-80°C) 영문 규격 통일.
  - **민감성 (Sensitivity)**: Moisture, Light, Air, Heat 키워드 단위 요약 (Sensitive 단어 제거).
  - **위험분류**: GHS 위험문구(예: H225: Highly flammable liquid) 전체 텍스트 추출.
* ✅ **사용자 체크 포인트**:
  - 이미 DB 시트에 존재하는 시약은 웹 접속을 생략하고 DB에서 즉시 당겨와 속도를 최적화합니다.
  - 특정 시약 정보를 새로고침하고 싶다면 DB 시트에서 해당 시약 행을 삭제한 후 동기화나 DB 수동 업데이트를 실행하세요.
  - 단종되거나 검색이 안 될 경우 **"제조사 홈페이지에서 검색 실패"** 로 표기됩니다.

### Step 4. SDS (PDF) 원본 자동 다운로드

* **프로세스**: 윈도우 파일 시스템 특수문자(`\`, `/`, `:`, `*`, `?`, `"`, `<`, `>`, `|`)를 정제하여 `시약명 (제조사, 제품번호).pdf` 포맷으로 sds/ 폴더에 자동 다운로드합니다.
* ✅ **사용자 체크 포인트**:
  - sds/ 폴더 내 PDF 파일과 엑셀 리스트를 정기적으로 대조해 보세요.
  - **[SDS 일괄 병합]** 버튼으로 전체 시약 SDS를 1개의 종합 책자 PDF로 합성할 수 있습니다.
  - 기존 SDS 로컬 파일이 있고 수정일이 180일 이내이면 동일 문서를 다시 내려받지 않습니다.

### Step 4-1. Lot별 COA/CoC 자동 다운로드

* **프로세스**: 오더북의 제조사, 카탈로그 번호, `Lot No.` 조합별로 공식 품질문서를 조회하고 검증된 PDF만 `coa/` 폴더에 저장합니다.
* ✅ **사용자 체크 포인트**:
  - 같은 제품이라도 Lot가 다르면 별도 문서를 내려받습니다.
  - Excel이 숫자형 Lot의 선행 0을 제거해 `0000494416`을 `494416`으로 표시하더라도 동일한 숫자 Lot로 비교합니다. 기존 PDF가 정상이라면 서명 URL이 달라져도 다시 다운로드하지 않습니다.
  - 숫자만으로 표기되어 일/월 순서를 확정할 수 없는 만료일은 잘못 추측하지 않고 빈 값으로 둡니다. COA 원문을 확인한 뒤 직접 입력해 주세요.
  - 다운로드 중 중단하면 브라우저 요청을 종료하고 임시 격리 폴더를 정리하므로 완료되지 않은 문서가 정식 `coa/` 폴더에 남지 않습니다.

### Step 5. ChemicalList.xlsx 최종 병합 저장 (In-Place Save)

* **프로세스**: 장시간 크롤링은 OneDrive 밖의 작업 복사본에서 수행합니다. 저장 직전에 최신 공유본을 다시 읽어 `base / ours / theirs` 셀 단위 3-way 비교를 수행하고, 비충돌 변경만 win32com Excel의 **Save()** 방식으로 병합합니다.
* ✅ **사용자 체크 포인트**:
  - 파일 저장이 완료될 때까지 잠시 기다려 주세요.
  - 동일 셀을 다른 사용자가 동시에 수정한 직접 충돌이 감지되면 저장하지 않고 최신 공유본에서 제한 횟수 내 자동 재시작합니다.
  - 실제 신규·업데이트·관리 범위 변경이 있을 때만 저장 직전 백업을 생성합니다. 변경사항이 없으면 원본을 저장하거나 백업하지 않습니다.
  - 저장 직전에 ChemicalList 시트를 활성화하므로 다음에 ChemicalList.xlsx를 열면 메인 ChemicalList 시트가 먼저 표시됩니다. 데이터 변경이 없더라도 현재 기본 열기 시트가 DB나 Guide라면 이 설정만 한 번 저장할 수 있습니다.

---

## 4. 🔒 OneDrive 공동 편집 & 파일 잠금 감지 정책

0. **ChemicalList 대상 파일 직접 선택**:
   - 기본 설정의 **ChemicalList 파일** 항목에서 실제 동기화 대상 `.xlsx` 파일을 직접 선택할 수 있습니다.
   - 선택한 경로는 동기화, DB 수동 업데이트, PDF 인쇄파일 생성 및 SDS 일괄 병합에 공통 적용됩니다.
   - 잘못된 파일 덮어쓰기를 막기 위해 `ChemicalList`와 `DB` 시트가 모두 있는 파일만 선택할 수 있으며 원본 오더북을 대상 파일로 선택할 수 없습니다.
   - 기존 설정에 선택 경로가 없으면 이전과 동일하게 원본 오더북 폴더의 `ChemicalList.xlsx`를 사용합니다.

1. **파일 사용 중 자동 감지 및 1분 간격 대기 루프**:
   - ChemicalList.xlsx가 다른 연구원에 의해 켜져 있거나 편집 중(잠금 상태)이면 동기화나 DB 업데이트 프로세스를 시작하지 않고 **1분 간격으로 사용 해제 여부를 자동 체크**하며 대기합니다.
   - 대기 시 상단 배지와 로그 창에 `"ChemicalList.xlsx 파일이 다른 프로그램(예: 엑셀)에서 사용 중입니다. 1분 후 사용 여부를 다시 확인합니다..."` 메시지가 표시됩니다.
   - 엑셀이 닫히는 즉시 작업이 자동으로 시작됩니다.

2. **사용자 소유 열(Column) 100% 비파괴 보존**:
   - 보관장소(Room), 캐비넷(Cabinet), 사용수량(Used), 수령상태(Status), 비고(Remarks) 등 연구원이 직접 작성하는 열은 동기화 시 프로그램이 절대로 수정하거나 덮어쓰지 않습니다.

3. **리스트 중간 행 삽입 및 합성/소분 시약 안전 지원**:
   - 연구원이 엑셀 중간에 새로운 행을 추가하거나, 자가 합성 시약/소분 시약/폐기 수량 등을 자유롭게 작성하셔도 됩니다.
   - 프로그램은 단순 행 번호가 아닌 **주문식별자(Order No.) 기반 실시간 위치 재탐색** 알고리즘을 사용하므로 위치가 밀려도 셀 덮어쓰기가 발생하지 않습니다.
   - 주문번호가 없는 기존 행도 제조사, 카탈로그 번호, Lot, 원본 제품명과 중복 순번을 결합한 레거시 논리 키로 공동편집 변경을 추적합니다. 수식만 들어 있는 빈 예약 행은 실제 데이터 행으로 오인하지 않습니다.

4. **자동 동기화 정지 상태 준수**:
   - 자동 실행 옵션으로 프로그램이 트레이에서 시작되더라도 동기화 간격이 0분이면 즉시 동기화를 실행하지 않습니다.
   - 종료 요청 시 진행 중인 Excel/네트워크 작업에 중단 신호를 보내고 실제 작업 스레드가 끝난 것을 확인한 뒤 프로그램을 종료합니다.

---

## 5. 💡 주요 기능별 상세 사용 설명서

| 기능 버튼 | 사용 목적 및 설명 | 실행 방법 |
| :--- | :--- | :--- |
| **🚀 수동 동기화 실행** | 오더북의 신규 입고 건을 ChemicalList.xlsx에 즉시 동기화하고 미등록 시약 정보를 수집합니다. | 메인 화면 **[🚀 수동 동기화 실행]** 클릭 |
| **🔄 DB 수동 업데이트** | DB 시트의 누락 항목(Missing), 특정 시약, 또는 전체 항목을 웹에서 다이렉트로 수집 갱신합니다. | **[🔄 DB 수동 업데이트]** ➔ 모드 선택 ➔ [실행] 클릭 |
| **📑 PDF 인쇄파일 생성** | 실물 관리용 엑셀 출력물을 PDF 문서로 변환합니다. (선택 열, 'O' 수령 항목 출력 지원) | **[📑 PDF 인쇄파일 생성]** ➔ 서식 지정 ➔ [생성] 클릭 |
| **📦 SDS 일괄 병합** | sds/ 폴더 내 개별 SDS PDF 파일들을 1개의 연속된 통합 SDS PDF 책자로 결합합니다. | **[📦 SDS 일괄 병합]** 클릭 |
| **📌 백그라운드 실행** | 프로그램을 시스템 트레이(시계 옆)로 최소화하여 백그라운드에서 주기적 동기화를 수행합니다. | **[📌 백그라운드 실행]** 클릭 |

> 자동 동기화가 정지되어 있거나 주기가 0분이면 백그라운드/트레이 시작만 수행하며 동기화 작업은 시작하지 않습니다.

---

## 6. ⚠️ 자주 묻는 질문 및 트러블슈팅 (Troubleshooting)

### Q1. "ChemicalList.xlsx 파일이 사용 중입니다. 1분 후 재시도..." 메시지가 떠요.
- **원인**: 본인 PC 또는 공유 폴더 상의 다른 사용자가 ChemicalList.xlsx를 엑셀 프로그램으로 열어둔 상태입니다.
- **해결**: 엑셀 프로그램을 닫으시면 1분 이내에 자동으로 잠금이 해제되고 동기화가 진행됩니다.

### Q2. 보관온도 값을 수정했는데 동기화 후 원래대로 돌아가요.
- **원인**: ChemicalList 시트의 보관온도는 DB 시트를 헤더 기반으로 참조하는 `INDEX/MATCH` 수식입니다.
- **해결**: 보관온도를 수정하고 싶다면 ChemicalList 시트 대신 **DB 시트에서 해당 시약의 보관온도를 수정**해 주세요.

### Q3. 특정 시약의 정보가 "제조사 홈페이지에서 검색 실패"로 나와요.
- **원인**: 제조사 사이트의 단종, 제품번호 오기재, 또는 Cloudflare 봇 차단 때문일 수 있습니다.
- **해결**: 엑셀에서 해당 항목을 수기로 보완하시거나, 잠시 후 **[DB 수동 업데이트]** 기능으로 해당 시약만 다시 실행해 보세요.

### Q4. 자가 합성 시약이나 소분 시약 정보가 지워지지 않나요?
- **답변**: 네. 소분 시약은 Order No.를 비워 두고 Product Name을 직접 입력하면 해당 이름을 DB 제품명으로 덮어쓰지 않습니다. Manufacturer, Catalog No., Lot No.는 원 시약 추적을 위해 그대로 입력할 수 있고 Remarks에는 `Aliquot` 및 소분 정보를 기록하면 됩니다. 자가 합성 시약처럼 제조사와 제품번호가 없는 항목도 기존 입력 텍스트를 유지합니다.

### Q5. 시약 리스트 중간에 행을 새로 추가해도 되나요?
- **답변**: 네, 주문식별자(Order No.) 기반으로 위치를 실시간 재탐색하므로 리스트 중간에 새로운 행을 삽입하거나 위치를 변경하셔도 완벽하게 보호됩니다.

### Q6. Revision Date에 시간이 보이거나 날짜가 어긋나면 어떻게 하나요?
- **답변**: Revision Date는 Excel 날짜 객체가 아니라 `YYYY-MM-DD` 텍스트로 저장합니다. 기존 datetime/Excel 일련번호 값은 다음 실제 변경 저장 시 원시 Excel 값에서 날짜를 계산하여 시간 없이 정리합니다.

### Q7. COA의 Expiration Date가 비어 있어요.
- **답변**: `08/12/2027`처럼 월/일 순서를 확정할 수 없는 숫자 날짜는 잘못된 만료일을 저장하지 않기 위해 자동 입력하지 않습니다. COA 원문에서 형식을 확인한 후 직접 기입하세요.

### Q8. 자동 동기화 정지인데 트레이에서 프로그램을 시작하면 어떻게 되나요?
- **답변**: 동기화 주기가 0분이면 프로그램만 트레이에서 시작하고 동기화는 실행하지 않습니다. 주기를 1분 이상으로 저장하고 자동 동기화를 활성화한 경우에만 시작 동기화와 주기 동기화가 동작합니다.

### Q9. ChemicalList.xlsx를 열었는데 DB나 Guide 시트가 먼저 보여요.
- **답변**: 수정된 프로그램은 저장할 때 ChemicalList 시트를 활성화합니다. 다음 동기화에서 현재 기본 열기 시트가 ChemicalList가 아니면 이 설정을 한 번 저장하며, 이후 파일을 열면 ChemicalList 시트가 먼저 표시됩니다.

### Q10. 오더북과 ChemicalList.xlsx가 서로 다른 폴더에 있어요.
- **답변**: 기본 설정의 **ChemicalList 파일 → 파일 찾기**에서 사용할 파일을 선택하세요. 이후 동기화, DB 업데이트, PDF 및 SDS 기능이 모두 해당 파일을 사용합니다.

### Q11. 백업 파일은 어디에 저장되고 오래된 파일은 언제 삭제되나요?
- **답변**: 백업은 ChemicalList 또는 OneDrive 폴더가 아니라 프로그램 실행파일과 같은 위치의 `backup` 하위 폴더에 저장됩니다. 소스 코드로 실행할 때는 `chem_manager_app/backup`을 사용합니다. 동기화를 시작할 때 이 폴더를 확인하여 파일 수정시각이 현재 날짜 기준 3개월 전과 같거나 더 오래된 `ChemicalList_backup_*.xlsx`만 삭제합니다. 다른 이름의 파일과 하위 폴더는 삭제하지 않습니다.

> 본 문서는 현재 코드의 3-way 병합, 선택형 ChemicalList 경로, 3개월 백업 보존, 헤더 기반 DB 수식, 3,000행 기본 관리 범위, Lot별 COA/CoC 처리, 날짜 전용 Revision Date 및 안전 종료 동작을 기준으로 갱신했습니다. Excel 파일 내부 `Guide` 시트는 본 갱신 대상에 포함하지 않았습니다.

---

## 👨‍💻 제작자 및 문의처
제작자 : Jeonghun Lee  
문의 : jhl22@hanyang.ac.kr
