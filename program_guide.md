# 화학물질 정보 및 SDS 자동 수집 프로그램 가이드

## 1. 프로그램 개요
이 프로그램은 시약 주문 시 작성되는 오더북(Excel)의 제조사와 제품번호를 바탕으로 각 제조사(Thermo Fisher, Sigma-Aldrich, TCI, Abcam)의 공식 홈페이지에 접속하여 화학물질 정보(시약명, CAS 번호, 보관온도, 위험분류, 민감성 등)를 수집하고, 관련 SDS(안전보건자료) PDF 문서를 자동으로 다운로드해 주는 자동화 툴입니다. 한 번 검색된 정보는 자체 로컬 데이터베이스(`chemical_db.xlsx`)에 저장되어 향후 작업 속도를 크게 단축시킵니다.

---

## 2. 프로그램 작동 프로세스 및 사용자 체크리스트

프로그램은 다음과 같은 단계로 작동합니다. 각 단계별로 사용자가 놓치지 않고 확인해야 할 **체크 포인트(✅)**를 안내해 드립니다.

### Step 1. 오더북 (입력 파일) 읽기 및 로컬 DB 검사
프로그램을 실행하면 가장 먼저 입력하신 오더북 엑셀 파일을 읽어옵니다. 그 후, 이전 검색 기록이 저장된 로컬 DB(`chemical_db.xlsx`)를 대조합니다.
* **프로세스**: 오더북의 [제조사명 + 제품번호]가 로컬 DB에 이미 존재한다면, 웹 접속(크롤링)을 생략하고 DB에서 즉시 정보를 불러와 속도를 최적화합니다.
* ✅ **사용자 체크 포인트**: 
    - 오더북의 열 이름(제조사, 제품번호 등)이 프로그램이 요구하는 규격과 일치하는지 확인하세요.
    - 만약 특정 시약의 정보를 **최신 내용으로 강제 업데이트**하고 싶다면, 프로그램 실행 전에 `chemical_db.xlsx` 파일을 열어 해당 약품의 행(Row)을 직접 삭제한 뒤 저장해 주셔야 웹에서 새로 검색을 시도합니다.

### Step 2. 타겟 제조사 분류
DB에 없는 새로운 제품일 경우, 오더북에 적힌 제조사명을 판별합니다.
* **프로세스**: 제조사가 **Thermo Fisher, Sigma-Aldrich(Aldrich), TCI, Abcam** 중 하나인지 확인합니다. (대소문자는 무시됩니다.)
* ✅ **사용자 체크 포인트**:
    - 만약 위 4개 제조사가 아니라면 웹 크롤링을 시도하지 않고 결과란에 **"직접 입력하세요"** 라고 표기됩니다. 완료 후 이 표기가 있는 행들은 사용자가 직접 구글링 등을 통해 수기 입력해야 합니다.

### Step 3. 웹 스크래핑 및 정보 자동 추출
분류가 완료되면 봇 우회 기능(SeleniumBase)이 탑재된 브라우저가 백그라운드에서 열리며 각 제조사 홈페이지에 접속합니다.
* **프로세스**: 
    - 시약명, CAS Number를 추출합니다.
    - **보관온도**: RT, Refrigerator (4도), Freezer (-20도), Deep freezer (-80도) 4가지 영문 포맷으로 통일합니다.
    - **위험분류**: GHS 기준 전체 영문 문구(예: `H225: Highly flammable liquid`)로 추출 및 번역합니다.
    - **민감성**: 긴 문장 대신 직관적인 **Moisture, Light, Air, Heat** 단어 단위 리스트로 요약 추출합니다.
* ✅ **사용자 체크 포인트**:
    - 인터넷 연결 상태와 방화벽 여부를 확인하세요. 연결이 불안정하면 크롤링이 중단될 수 있습니다.
    - 사이트에서 해당 제품이 단종되었거나 검색되지 않으면 결과에 **"제조사 홈페이지에서 검색 실패"**로 표기됩니다. 이 항목들은 나중에 결과 엑셀에서 확인하여 누락되지 않도록 직접 처리하셔야 합니다.
    - 알드리치(Sigma-Aldrich) 제품번호 뒤에 용량 표기(예: `909270-10G`의 `-10G`)가 기재되어 있다면 프로그램이 이를 자동으로 잘라내고 검색(`909270`)하지만, 특이한 표기법이 섞여 검색 실패가 뜨지 않았는지 점검해 주세요.

### Step 4. SDS (PDF) 원본 다운로드
상세 정보 추출이 끝나면 즉시 해당 제품의 SDS 문서 원본을 요청하여 다운로드합니다.
* **프로세스**: 윈도우 파일 시스템에서 오류를 유발하는 특수문자(`?`, `"`, `|` 등)를 정제하고 공백을 언더바(`_`)로 치환한 뒤, `시약명.pdf` 형태로 지정된 `sds/` 폴더 내에 저장합니다. 
* ✅ **사용자 체크 포인트**:
    - 다운로드 과정에서 TCI 등 일부 사이트는 보안 인증 한계로 간혹 실패할 수 있습니다. 프로그램 구동 완료 후 `sds/` 폴더를 열어 추출된 엑셀 명단과 PDF 파일 개수/이름이 일치하게 저장되었는지 대조해 보세요.
    - SDS 다운로드 URL 갱신이 제조사 측에서 일어났을 경우 파일이 열리지 않거나 0KB로 다운받아질 수 있으니 정기적으로 확인해 주는 것이 좋습니다.

### Step 5. 최종 엑셀 파일 저장
모든 추출 작업과 다운로드가 완료되면 그 결과를 출력용 엑셀 파일과 로컬 DB(`chemical_db.xlsx`)에 최종 저장합니다.
* ✅ **사용자 체크 포인트**:
    - **[중요]** 프로그램이 동작하여 결과물을 기록할 때, `chemical_db.xlsx`나 오더북 엑셀 파일이 **컴퓨터(엑셀 프로그램)에 열려있으면 파일 쓰기 에러(Permission Error)가 발생하여 튕깁니다.** 반드시 프로그램을 실행하기 전 관련 엑셀 파일들을 모두 닫아주세요.

---

## 3. ⚠️ 요약: 필수 주의사항 (Precautions)

1. **파일 닫기**: 프로그램 실행 전 반드시 관련 엑셀 파일(입력용 오더북, 로컬 DB 엑셀)이 모두 닫혀있는지 확인하세요.
2. **정보 갱신 필요시**: 기존에 한 번 검색했던 약품의 정보를 다시 새로고침하여 받고 싶다면, 반드시 `chemical_db.xlsx` 파일을 열어 해당 약품의 행을 지우고 저장한 뒤 프로그램을 실행하세요.
3. **봇 방어 (Cloudflare) 정책**: 각 제조사(특히 Sigma-Aldrich, TCI, Abcam 등)의 봇 방어 시스템이 일시적으로 강화되어 간헐적 접속 실패가 발생할 수 있습니다. 엑셀 결과물에서 "제조사 홈페이지에서 검색 실패"가 비정상적으로 많이 뜬다면, 잠시 기다렸다가 실행하거나 네트워크 상태를 재점검하세요.
4. **결과 검수**: 자동화 툴이 완료된 후, 결과 엑셀 파일의 필터 기능을 사용하여 빈칸이나 "검색 실패", "직접 입력하세요" 로 표시된 부분만 빠르게 걸러내어 수동으로 보완해 주시면 완벽한 시약 관리가 가능합니다.
