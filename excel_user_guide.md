# 📖 연구실 시약 및 주문 대장 엑셀 작성·사용 가이드 (일반 사용자용)

본 가이드는 연구실 구성원(연구원 및 일반 사용자)이 **Orderbook (주문 대장)** 및 **ChemicalList.xlsx (시약 현황 관리판)** 엑셀 파일을 작성할 때 정확한 데이터를 유지하고 자동 동기화 프로그램과 오류 없이 연동되도록 돕기 위한 수칙입니다.

---

## 📝 1. Orderbook (주문 대장) 작성 방법 및 주의사항

연구실에서 새로운 시약을 구매/신청할 때 작성하는 대장입니다.

### 📌 주요 항목 기입 규칙
1. **번호 (주문 번호)**: 고유한 주문 식별 번호 (예: `26-0001`, `26-0063`)를 기입합니다.
2. **날짜**: 주문 일자를 `YYYY-MM-DD` 형식으로 기입합니다.
3. **주문자**: 신청 연구원의 이름을 기입합니다.
4. **회사 (제조사)**: 공식 제조사 영문명을 정확히 입력합니다.
   - *추천 표기법*: `Thermo Fisher`, `Sigma-Aldrich`, `TCI`, `Abcam` 등
5. **품목명 (시약명)**: 시약의 정확한 이름(영문/국문)을 기입합니다.
6. **품번 (Catalog No. / 카탈로그 번호)**: 제조사 공식 카탈로그 번호를 기입합니다.
   - *주의*: 앞자리 `0`이나 하이픈(`-`)이 누락되지 않도록 주의하세요.
7. **용량 (Package Size)**: `500 mL`, `25 g`, `1 L` 등 포장 용량을 입력합니다.
8. **수량 (Quantity)**: 주문한 수량을 숫자로 입력합니다.
9. **수령확인 (Status)**: 시약이 연구실에도 착하여 수령 완료되면 **`O`** 또는 **`ㅇ`**을 기입합니다.
    - *참고*: 수령 확인이 입력된 시약만 현황판에 정상 비치 품목으로 등록 및 출력됩니다.
10. **Lot No.**: 제조사 용기 또는 납품 문서에 표시된 Lot 번호를 정확히 기입합니다.
    - 같은 제품을 여러 번 주문해 Lot가 다르면 각 주문 행에 해당 Lot를 따로 입력해야 올바른 COA가 연결됩니다.
    - 숫자형 Lot는 가능하면 Excel 셀을 텍스트 형식으로 지정하여 `0000494416`과 같은 선행 0을 보존하세요. 기존 셀에서 `494416`으로 보이더라도 프로그램은 숫자 부분이 같으면 동일 Lot로 판정하여 정상 COA를 반복 다운로드하지 않습니다.
    - 오더북의 `주문회사`와 제조사를 의미하는 `회사`는 서로 다른 정보입니다. COA/SDS 조회에는 제조사인 `회사` 값과 품번을 사용합니다.

### ⚠️ Orderbook 작성 시 절대 금지 및 주의사항
- ❌ **중간 빈 줄(Blank Row) 금지**: 데이터 행 사이에 완전히 비어 있는 줄을 남겨두지 마세요. 프로그램이 데이터를 읽다가 중간에 중단될 수 있습니다.
- ❌ **제조사 및 품번 오탈자 주의**: 제조사 이름이나 카탈로그 번호에 띄어쓰기 오탈자가 있으면 홈페이지 크롤링 검색에 실패할 수 있습니다.

---

## 🧪 2. ChemicalList.xlsx (시약 현황 관리판) 사용 가이드

연구실 시약장에 비치된 시약 현황과 보관 위치, 사용량을 관리하는 메인 시트입니다.

### 📌 시트 구성 설명
- **`ChemicalList` 시트 (메인 관리 시트)**: 연구원이 직접 보관장소, 캐비넷, 사용수량 등을 기입하고 확인하는 화면입니다.
- **`index` 시트 (시약 보관 장소 정의 색인 시트)**: 연구실의 유효한 보관장소(Room), 보관온도(Storage Temp.), 캐비넷(Cabinet)의 표준 리스트 범위를 정의하고 관리하는 마스터 색인 시트입니다.
- **`DB` 시트 (마스터 데이터베이스)**: 크롤링 및 수집된 화학물질의 위험성, CAS No., 보관온도, 민감성, SDS 링크 등이 저장되는 데이터베이스 시트입니다.
- **`Guide` 시트**: 본 가이드 내용이 요약되어 포함된 안내 시트입니다.

> 현재 Excel 파일의 `Guide` 시트는 별도 확인·수정 대상입니다. 이번 프로그램 및 Markdown 가이드 갱신 과정에서는 Excel 내부 Guide 시트를 자동 수정하지 않습니다.

### ✍️ 연구원이 직접 기입/수정하는 항목
- **Room (보관 장소)**: 시약이 비치된 연구실 방 번호 (예: `501호`, `502호`)
- **Cabinet (캐비넷 / 보관함)**: Room 및 보관온도 조건에 맞춘 시약장 번호
- **Used (사용 수량)**: 시약을 사용하거나 소진했을 때 사용 수량 업데이트 (전체 수량 `Quantity`와 비교하여 `Status` 자동 결정)
- **Remarks (비고)**: 개봉일자, 특이사항, 사용자 메모 등
- **Lot No.**: 실제 용기의 Lot와 오더북 값이 다를 때는 원문을 확인한 뒤 올바른 Lot를 유지합니다.
- **Expiration Date**: 자동 추출되지 않거나 숫자 날짜의 일/월 순서가 모호한 경우 COA 원문을 확인하여 `YYYY-MM-DD`로 입력합니다.

### ⚠️ ChemicalList.xlsx 작성 시 절대 주의사항
- ❌ **DB 조회 수식 셀 직접 삭제/수정 금지**:
  - `Product Name`, `Storage Temp.`, `CAS No.`, `Sensitivity`, `Signal Word`, 위험정보, SDS Link/Local Path 등은 DB 시트와 헤더 기반 `INDEX/MATCH` 수식으로 자동 연동되어 있습니다.
  - DB 열 순서가 바뀌어도 헤더 이름을 기준으로 참조하므로, 헤더 이름 자체를 임의로 변경하거나 삭제하지 마세요.
  - 특정 시약의 보관온도를 기본값과 다르게 지정하고 싶다면 `ChemicalList` 시트의 수식을 지우지 마시고, **`DB` 시트에서 해당 시약의 보관온도를 수정**하셔야 수식이 깨지지 않습니다.
- ❌ **DB 시트 행 삭제 금지**: DB 시트의 행을 삭제하면 `ChemicalList` 시트 수식에 `#REF!` 오류가 발생합니다.
- 💡 **자가 합성 시약 / 소분 시약 기입 규칙**:
  - **소분 시약(Aliquot)**은 `Order No.`를 비워 두고 Manufacturer, Catalog No., Lot No. 등 원 시약의 추적 정보는 그대로 입력합니다.
  - Product Name에는 소분 시약을 구분할 이름을 직접 입력합니다. 주문번호가 없고 Product Name이 직접 입력된 값이면 프로그램이 DB 제품명으로 덮어쓰지 않습니다.
  - Remarks에는 반드시 `Aliquot`이라고 적고, 필요하면 소분일, 농도, 용량, 원 용기 위치 또는 원 시약 정보를 함께 기록하세요. 프로그램은 이 Remarks를 덮어쓰지 않습니다.
  - 주문번호가 없는 기존 행이라도 Product Name이 이미 프로그램 수식인 정규 시약 행은 DB 연동을 유지하기 위해 수식이 갱신될 수 있습니다.
  - 제조사나 카탈로그 번호가 없는 자체 합성 시약은 제조사와 품번을 비워두고 시약명과 위치 정보만 기입하면 기존 텍스트가 100% 그대로 안전하게 유지됩니다.
- 📄 **COA/CoC 열 사용 규칙**:
  - `COA Link`와 `COA Local Path`에는 해당 Lot의 공식 문서 링크와 로컬 파일 경로가 기록됩니다.
  - Abcam이 COA 대신 CoC를 제공하면 파일명에 `CoC`가 표시되고 두 셀이 주황색으로 표시됩니다. 셀 메모에는 CoC 안내와 Datasheet 링크/로컬 경로가 기록됩니다.
  - 같은 시약이라도 Lot가 다르면 다른 COA가 연결되어야 하므로 다른 행의 COA 경로를 복사해 사용하지 마세요.

---

## 🔄 3. 프로그램 자동 동기화 관련 유의사항

1. **파일을 켜놓고 작업해도 안전합니다**:
   - `ChemicalList.xlsx`가 Excel에서 편집 중이면 프로그램은 강제로 덮어쓰지 않고 1분 간격으로 잠금 해제를 확인합니다. 수동 작업은 사용자가 중단할 때까지, 자동 작업은 다음 자동 주기 전까지 정해진 횟수만 대기합니다.
   - 장시간 크롤링은 OneDrive 밖의 작업 복사본에서 수행하며, 저장 직전에 최신 공유본과 3-way 비교하여 비충돌 셀만 병합합니다.
2. **백업 파일 자동 생성**:
   - 실제 신규·업데이트·관리 범위 변경이 있을 때만 저장 직전에 프로그램 실행파일과 같은 위치의 `backup/` 폴더에 `ChemicalList_backup_YYYYMMDD_HHMMSS_ffffff.xlsx` 형태로 백업됩니다. ChemicalList 파일이나 OneDrive 공유본의 하위 폴더에는 새 백업을 만들지 않습니다.
   - 변경사항이 없으면 파일을 저장하지 않고 백업도 생성하지 않습니다.
   - 동기화를 시작할 때 프로그램 하위 `backup/` 폴더에서 파일 수정시각이 현재 기준 3개월 전과 같거나 오래된 `ChemicalList_backup_*.xlsx`를 자동 삭제합니다. 다른 파일과 하위 폴더는 삭제하지 않습니다.
3. **ChemicalList 파일 직접 선택**:
   - 프로그램 기본 설정에서 오더북과 별도로 사용할 ChemicalList.xlsx를 선택할 수 있습니다.
   - 선택한 파일은 `ChemicalList`와 `DB` 시트를 모두 포함해야 하며 원본 오더북과 같은 파일은 선택할 수 없습니다.
   - 동기화, DB 수동 업데이트, PDF 출력 및 SDS 병합은 모두 이 선택 경로를 사용합니다.
4. **자동 동기화 정지 상태**:
   - 자동 동기화 간격이 0분이거나 화면에서 자동 동기화를 정지하면 트레이 시작 옵션으로 실행되어도 동기화를 즉시 시작하지 않습니다.
5. **관리 범위 자동 확장**:
   - 조건부 서식과 데이터 유효성 검사는 기본 3,000행까지 준비됩니다.
   - 실제 데이터가 2,500행을 초과하면 마지막 데이터 행보다 최소 1,000행 큰 범위를 확보한 뒤 500행 단위로 올림합니다. 신규 시약의 마지막 행 판단은 `Product Name` 열을 기준으로 합니다.
6. **Revision Date 형식**:
   - DB의 Revision Date는 시간 없이 `YYYY-MM-DD` 텍스트로 기록됩니다. Excel 수식 입력줄에 시간이 따라붙지 않도록 날짜 객체 대신 날짜 전용 문자열을 사용합니다.

7. **프로그램 종료 시 대기**:
   - 동기화·DB 저장·COA/SDS 작업 중 종료를 선택하면 프로그램은 먼저 중단 신호를 보내고 Excel 및 네트워크 작업이 실제로 끝날 때까지 종료를 미룹니다. 종료 중에는 ChemicalList.xlsx나 다운로드 파일을 강제로 삭제하거나 이동하지 마세요.
8. **기본 열기 시트**:
   - 프로그램이 ChemicalList.xlsx를 저장할 때 메인 `ChemicalList` 시트를 활성화합니다. 이후 파일을 열면 DB나 Guide가 아니라 ChemicalList 시트가 먼저 표시됩니다.
   - 데이터 변경이 없어도 기존 파일의 기본 열기 시트가 ChemicalList가 아니면 이 설정을 반영하기 위해 한 번 저장하고 백업할 수 있습니다. 설정이 반영된 뒤에는 같은 이유로 반복 저장하지 않습니다.

> 이 문서는 현재 프로그램 코드의 동작을 기준으로 갱신되었으며, 기존 상세 작성 규칙과 사용자 주의사항을 유지한 상태에서 자동 동기화, 공동편집, DB 수식, COA/CoC, 관리 범위 및 날짜 처리 변경사항을 추가했습니다. Excel 파일 내부 `Guide` 시트는 수정하지 않았습니다.

---

## ✉️ 제작자 및 문의처

- **제작자**: Jeonghun Lee
- **문의 이메일**: `jhl22@hanyang.ac.kr`
